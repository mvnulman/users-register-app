# Copy this file to `.env` and replace the placeholder with your real connection string.
# Connection string for MongoDB (example only - never commit real credentials).

DATABASE_URL="mongodb://localhost:27017/api"